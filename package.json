{
  "name": "function-get-iplayer",
  "version": "0.1.0",
  "description": "OpenFaaS tasks for using get_iplayer",
  "main": "./function/handler.js",
  "scripts": {
    "postversion": "git push --tags && git push",
    "test": "npm run -s test:lint",
    "test:lint": "semistandard"
  },
  "author": "Simon Emms <simon@simonemms.com>",
  "license": "MIT",
  "dependencies": {
    "dropbox": "^4.0.1",
    "glob": "^7.1.2",
    "isomorphic-fetch": "^2.2.1",
    "js-yaml": "^3.11.0"
  },
  "devDependencies": {
    "json": "^9.0.6",
    "semistandard": "^12.0.1"
  }
}
